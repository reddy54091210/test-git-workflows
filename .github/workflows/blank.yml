name: hello world
on: 
  push:
    branches:
      -main
  pull-request: 
    branches:
      -main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Build Step
       run: echo "demo build"
  
       
  
